[package]
name = "evade-game"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.19.0"
rand = "0.10"

# 디버그 빌드에서도 게임이 부드럽게 돌도록 의존성만 최적화
[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
